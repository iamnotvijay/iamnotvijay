#include <stdio.h>
int main() {
int a = 50;      BOOL CALLBACK ChildProc(HWND hwnd, UINT Message, WPARAM wParam, LPARAM lParam) {
	switch(Message) {
		case : {
			break;
		}
		default:
			return false;
	}
	return true;
}

